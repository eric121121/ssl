# 动目标模块使用说明

## 1. 模块功能概述

`algorithms/moving_target.py` 提供了动目标支持功能，允许目标节点在SSL构建过程中按照设定的速度移动。

### 主要功能：
- 检测目标是否为可移动目标
- 根据速度和时间更新目标位置
- 支持批量更新多个目标位置
- 支持动画展示动目标移动过程

## 2. 核心函数说明

### 2.1 判断目标是否可移动

```python
from algorithms import is_movable_target

target = {
    'coord': (100, 200),
    'vx': 5.0,   # X方向速度 (km/h)
    'vy': 3.0,   # Y方向速度 (km/h)
    'vh': 0.0    # 高程变化速度
}

if is_movable_target(target):
    print("目标可以移动")
else:
    print("目标是静止的")
```

**判断标准**：vx、vy、vh 中至少有一个不为0

### 2.2 更新单个目标位置

```python
from algorithms import update_target_position

# 目标初始位置 (100, 200)，速度 vx=5, vy=3
target = {
    'coord': (100, 200),
    'vx': 5.0,
    'vy': 3.0,
    'vh': 0.0
}

# 经过10秒后的新位置
new_target = update_target_position(target, time_elapsed=10.0)
print(f"新位置: {new_target['coord']}")  # 输出: (150.0, 230.0)
```

**计算公式**：
- 新X坐标 = 原X坐标 + vx × time_elapsed
- 新Y坐标 = 原Y坐标 + vy × time_elapsed

### 2.3 批量更新多个目标位置

```python
from algorithms import update_all_positions

# 多个目标
targets = {
    't1': {'coord': (100, 200), 'vx': 5.0, 'vy': 3.0, 'vh': 0.0},
    't2': {'coord': (50, 100), 'vx': -2.0, 'vy': 4.0, 'vh': 0.0},
    't3': {'coord': (0, 0), 'vx': 0.0, 'vy': 0.0, 'vh': 0.0}  # 静止目标
}

# 批量更新（经过5秒）
updated_targets = update_all_positions(targets, time_elapsed=5.0)

for tid, target in updated_targets.items():
    print(f"{tid}: {target['coord']}")
```

### 2.4 检查是否存在可动目标

```python
from algorithms import has_movable_targets

targets = {
    't1': {'coord': (100, 200), 'vx': 5.0, 'vy': 3.0, 'vh': 0.0},
    't2': {'coord': (50, 100), 'vx': 0.0, 'vy': 0.0, 'vh': 0.0}
}

if has_movable_targets(targets):
    print("存在可动目标")
else:
    print("所有目标都是静止的")
```

## 3. 在主窗口中使用动目标动画

### 3.1 启动动画

在 `app/main_window.py` 中已经集成了动目标动画功能：

```python
# 在主窗口实例中调用
self.start_moving_target_animation()
```

**功能说明**：
- 自动检测是否存在可动目标
- 保存目标初始位置
- 启动定时器，按时间步长更新目标位置
- 每步更新后自动重新运行SSL构建算法
- 实时刷新画布显示

### 3.2 动画参数配置

```python
# 在 __init__ 方法中配置
self.animation_duration = 60.0    # 动画总时长（秒）
self.animation_time_step = 1.0    # 时间步长（秒）
```

### 3.3 停止动画

动画会在以下情况自动停止：
- 达到设定的动画时长
- 手动调用 `stop_moving_target_animation()`

停止后会自动恢复目标到初始位置。

## 4. 数据库字段要求

目标节点表 (`target_nodes`) 需要包含以下字段：
- `vx`: X方向速度 (float)
- `vy`: Y方向速度 (float)
- `vh`: 高程变化速度 (float)

这些字段已经在 `get_all_targets()` 方法中被读取和使用。

## 5. 使用场景示例

### 场景1：静态SSL构建（传统方式）
```python
# 目标速度均为0，行为与传统方式相同
targets = {
    't1': {'coord': (100, 200), 'vx': 0, 'vy': 0, 'vh': 0}
}
```

### 场景2：动态SSL构建
```python
# 目标以一定速度移动
targets = {
    't1': {'coord': (100, 200), 'vx': 5.0, 'vy': 3.0, 'vh': 0.0}
}

# 启动动画，观察目标移动过程中SSL方案的变化
self.start_moving_target_animation()
```

### 场景3：混合目标（部分移动，部分静止）
```python
targets = {
    't1': {'coord': (100, 200), 'vx': 5.0, 'vy': 3.0, 'vh': 0.0},  # 移动
    't2': {'coord': (50, 100), 'vx': 0.0, 'vy': 0.0, 'vh': 0.0},   # 静止
    't3': {'coord': (0, 0), 'vx': -2.0, 'vy': 4.0, 'vh': 0.0}      # 移动
}
```

## 6. 注意事项

1. **速度单位**：vx/vy/vh 的单位是 km/h，时间单位是秒
2. **坐标系**：使用与系统一致的坐标系（X向右，Y向上）
3. **性能考虑**：动画过程中会频繁重新运行算法，建议：
   - 适当增大 `animation_time_step`（如2-5秒）
   - 控制目标数量
   - 使用较快的算法（如遍历搜索而非MOPSO）

## 7. 扩展功能

如需添加动画控制按钮，可在控制面板中添加：

```python
# 在控制面板中添加按钮
self.animate_button = QtWidgets.QPushButton("启动动目标动画")
self.animate_button.clicked.connect(self.start_moving_target_animation)
self.control_panel_layout.addWidget(self.animate_button)

self.stop_animate_button = QtWidgets.QPushButton("停止动画")
self.stop_animate_button.clicked.connect(self.stop_moving_target_animation)
self.control_panel_layout.addWidget(self.stop_animate_button)
